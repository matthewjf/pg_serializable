require 'spec_helper'

RSpec.describe "validations" do
  context "attributes" do
    it "must have column"
  end

  context "associations" do
    it "must have the association"
    it "must not have cycles"
  end
end
